# Python
__pycache__/
*.py[cod]
*.pyo

# Environment (절대 커밋 금지)
.env
.env.*
!.env.example
backend/.env
ai/docker/.env

# 내부 공유 문서 (PUBLIC 레포 비공개 — 로컬에만 보관)
# 발표 대비 심화/방어·내부 평가 보고·주간 보고·리팩토링 계획·결정 기록.
# 실제 보안 약점 서술이 포함돼 있어 공개 레포에 두지 않는다.
/DECISIONS.md
/TASK_LOGIC.md
/TASK_LOGIC_DEEP.md
/CHART_FOLLOWUP_DEEP.md
/AGENT_IMPROVEMENTS.md
/SERVICE_TEST_REPORT.md
/TRIAGE_REFACTOR_PLAN.md
/WEEKLY_REPORT_2026-0615_0619.md
/PRESENTATION_DEFENSE.md
/ai/agents/EVALUATION_PLAN.md

# IDE
.vscode/
.idea/
.cursor/

# OS
.DS_Store
Thumbs.db

# Frontend
frontend/**/node_modules/
frontend/**/dist/
frontend/**/.env
*.tsbuildinfo
# vet-web 의 tsconfig.node 가 composite emit 라 vite.config 를 .js/.d.ts 로 재생성한다.
# 소스는 vite.config.ts 이며 빌드(tsc -b) 때 자동 생성되므로 산출물은 커밋하지 않는다.
frontend/vet-web/vite.config.js
frontend/vet-web/vite.config.d.ts

# Python virtual env
venv/
.venv/

# Local agent instructions
AGENTS.md

# Claude Code AI 메모리 (로컬 전용)
.claude/


*.log
frontend/guardian-web/*.log

# Triage RAG 원본/임베딩 데이터 — git 비커밋, DB 덤프로 공유 (vet_triage.json은 추적 유지)
backend/data/triage/raw/
backend/data/triage/*.zip
backend/data/triage/*.dump
backend/data/triage/*.sql
backend/data/triage/*.sql.gz
backend/data/triage/*.jsonl
backend/data/triage/*.jsonl.gz
backend/data/triage/*.gz

# Evaluation/Validation 개발 전용 파일 — 로컬 전용, 커밋 금지
ai/agents/test_data/
# 평가 실행 산출물(리포트/결과) — 생성물이라 추적 안 함. 데이터셋(*_dataset.json, PetEVAL.json)은 유지.
backend/data/validation/*_report.html
backend/data/validation/*_report.json
backend/data/validation/*_result.json

