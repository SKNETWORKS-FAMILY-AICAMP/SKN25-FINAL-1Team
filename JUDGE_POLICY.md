# Judge Agent 정책 (JUDGE_POLICY.md)

> **한 줄 정의**: Judge Agent는 *환자 안전 검증기*가 아니라, **AI 대화 시스템의 운영 품질을 모니터링하는 계층**이다.

---

## 1. 목적

Judge Agent는 **시스템 품질 모니터링(System Quality Monitoring)** 을 수행한다. 개별 환자의 안전이나 의료적 정답을 판단하지 **않는다.** 대신 "우리 AI 대화 시스템이 시간이 지나도 설계 의도대로(효율적·일관적으로) 작동하고 있는가"를 표본 점검한다.

비유: Judge는 **콜센터 QA 담당자**와 같다. 상담원이 고객을 잘 응대했는지 *통화 품질*을 표본 평가하지, 고객의 병을 진단하지 않는다.

---

## 2. 평가 항목 (운영 품질 지표)

| 지표 | 의미 | 산출 방식 |
|---|---|---|
| **문진 완전성** | 필수 항목을 대화로 잘 수집했는가 | LLM 평가(0~10) |
| **질문 효율성** | 불필요한 반복 질문 없이 효율적으로 수집했는가 | LLM 평가(0~10) |
| **대화 턴 수** | 보호자 응답까지 걸린 턴 수 | **코드로 직접 계산**(객관) |
| **응답 일관성** | 대화 흐름과 구조화 결과가 논리적으로 일관되는가 | LLM 평가(0~10) |
| **구조화 품질** | 일상어를 구조화 필드로 잘 변환했는가 | LLM 평가(0~10) |

판정: `HEALTHY`(네 점수 모두 7.0+) / `NEEDS_REVIEW`(하나라도 7.0 미만 → **프롬프트·대화 설계 점검 대상**, 환자 위험 신호 아님).

코드 기준: [ai/agents/judge.py](ai/agents/judge.py)

---

## 3. 운영 활용 방식

- **샘플링 실행**: 모든 예약마다 돌리지 않고 일부 표본만 평가한다(`JUDGE_SAMPLE_RATE`, 기본 1/5). 품질 모니터링은 전수가 아니라 표본으로 충분하며 비용을 통제한다. (발표 시연용으로 매번 보려면 `JUDGE_SAMPLE_RATE = 1`)
- **저장하지 않는다**: Judge 결과는 **audit log에만** 기록되고 DB에 저장하지 않는다(`[JudgeAudit]`). 환자 진료 데이터가 아니라 **시스템 운영 지표**이기 때문이다.
- **독립 인스턴스**: Triage와 같은 모델 인스턴스의 self-enhancement(자기강화) 편향을 줄이기 위해 별도 호출로 평가한다.
- **활용처**: 운영자가 로그를 모아 "최근 NEEDS_REVIEW 비율이 올라갔다 → 프롬프트를 손봐야 한다" 같은 **시스템 개선 신호**로 쓴다. 개별 환자 처치에는 관여하지 않는다.

---

## 4. Judge가 하지 않는 것

- ❌ 환자 안전 판정
- ❌ 응급도 판단
- ❌ 진단 정확성 판단
- ❌ 개별 진료 의사결정 개입
- ❌ DB 저장 / 수의사 화면 노출

---

## 5. Validation과 Judge의 명확한 분리

| | **Validation** | **Judge** |
|---|---|---|
| 대상 | 개별 케이스의 **산출물** | **시스템 전체**의 작동 품질 |
| 질문 | "이 결과, 수의사가 봐야 할 지점 있나?" | "우리 AI가 설계대로 잘 돌고 있나?" |
| 빈도 | 매 케이스 | 표본(1/5) |
| 저장 | DB(수의사 참고) | audit log만 |
| 비유 | 출고 전 **품질 검사(QC)** | 공정 전체 **품질 관리(QA) 모니터링** |

→ 둘은 역할이 다르므로 "AI가 AI를 평가하는 중복 아닌가?"라는 질문에 **"검증 계층(Validation)과 모니터링 계층(Judge)은 목적이 다르다"** 고 답할 수 있다.
