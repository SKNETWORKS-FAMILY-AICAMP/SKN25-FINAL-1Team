# 발표 방어 문서 (PRESENTATION_DEFENSE.md)

> **핵심 포지션**: MediPaw는 *의료 진단 AI가 아니다.* AI 결과를 그대로 쓰지 않고 **검증 계층 + 인간(수의사) 검토 절차**를 둔 **업무 보조 시스템**이다. 모든 의료 최종 판단은 수의사가 한다.

이 한 문장으로 모든 질문을 방어한다. AI는 **구조화·초안·신호 표시**까지만 하고, **판단은 사람이 한다(Human-in-the-Loop)**.

---

## 1. 예상 질문 & 답변

### Q1. AI가 의료 진단을 하나요?
**A.** 하지 않습니다. AI의 역할은 세 가지로 제한됩니다 — ① 보호자의 자연어를 **구조화**하고, ② 응급 가능성을 시사하는 **신호(Red Flag)를 식별**하고, ③ 수의사가 검토할 **차트 초안(draft)** 을 만드는 것입니다. 진단 확정·응급도 결정·처방은 **전부 수의사**가 합니다. 시스템 구조상 AI 산출물은 항상 수의사의 검토·수정·승인을 거치도록 설계돼 있습니다(Human-in-the-Loop).

### Q2. 응급도 기준은 무엇이고, 누가 만들었나요?
**A.** 두 가지를 구분해야 합니다.
- **AI가 하는 것**: 보호자 표현에서 응급 가능성을 시사하는 **신호를 감지**합니다. 이는 "숨을 못 쉰다, 의식이 없다, 계속 경련한다, 피를 많이 흘린다" 같은 **사람이 만든 표현 사전(rule-based lexicon)** 에 대한 매칭입니다 — LLM의 주관적 점수가 아닙니다.
- **수의사가 하는 것**: 실제 응급도 판정과 처치 결정.
- 즉 AI는 "응급이다"라고 **판정하지 않고**, "응급 신호가 감지되었으니 수의사가 보라"고 **표시**만 합니다. 기준의 주체는 AI가 아니라, 운영 규칙(표현 사전)과 최종적으로 수의사입니다.

### Q3. 수의사 검증(임상 검증)을 받았나요?
**A.** 저희는 **진단 알고리즘의 의학적 정확도를 주장하지 않습니다.** 따라서 "진단 정확도 임상 검증"은 저희 시스템의 적용 대상이 아닙니다. 저희가 보장하는 것은 *의학적 정답*이 아니라 **업무 흐름의 안전성** — 즉 ① 정보 누락 점검, ② AI 산출물 간 모순 점검, ③ 응급 신호 표시, ④ 인간 검토 강제입니다. 의학적 판단은 시스템 밖, 수의사에게 있습니다. (이 경계가 곧 규제·책임 측면의 방어선입니다 — 아래 4장 FDA CDS 참고.)

### Q4. 보호자는 의료용어를 안 쓰는데 어떻게 응급도를 판단하나요?
**A.** 바로 그래서 **판단이 아니라 구조화·감지**로 설계했습니다. AI는 "토하고 기운 없어요" 같은 **일상어**를 받아 구조화 필드(주증상·발현시점 등)로 변환하고, 응급 신호 사전도 **일상 표현 기준**("숨을 못 쉰다" 등)으로 만들었습니다. 모호하면 AI가 되묻습니다. 그래도 불확실하면 **보수적으로 신호를 표시**하고 수의사에게 넘깁니다. 일상어의 모호함을 AI가 단독으로 해소하지 않고, **수의사 검토로 보정**하는 것이 핵심입니다.

### Q5. AI가 AI를 평가하는 것 아닌가요?
**A.** **Validation은 AI(LLM)를 쓰지 않습니다.** 4개 점검(데이터 완전성, 문진-차트 일치도, 예약 안전성, 응급신호 정합성) **전부 규칙·산수**입니다 — 필드 개수 세기, 키워드 일치율, 응급도 숫자 비교. 같은 입력이면 항상 같은 결과(재현 가능)라 "AI가 AI를 판정한다"는 구조가 성립하지 않습니다. AI가 만든 결과를 **사람이 정한 숫자 기준**으로 거르는 구조입니다.
- **Judge**만 LLM을 쓰는데, 이건 *환자*가 아니라 *시스템 운영 품질*(대화 효율·구조화 품질)을 표본 모니터링하는 용도입니다(콜센터 QA). 학계에서 확립된 LLM-as-a-Judge 기법(Zheng et al. 2023)이고, **환자 안전 판정엔 쓰지 않습니다.**
- 그리고 **최종 신뢰의 주체는 AI가 아니라 수의사**입니다. 검증은 수의사 검토를 *유도*하는 보조 신호이지 최종 승인이 아닙니다.

### Q6. Validation 점수는 왜 신뢰할 수 있나요?
**A.** "신뢰하라"가 아니라 **"검증은 검토를 유도하는 알림일 뿐"** 이 정답입니다. 그래도 근거를 대면 — **전부 결정론적**이기 때문입니다.
- 완전성 = 수집된 필수필드 ÷ 5. **필드 존재 여부**라는 객관 사실.
- 문진-차트 일치도 = 문진 증상 키워드가 차트에 등장한 비율(문자열 매칭). 50% 미만이면 알림.
- 응급신호 정합성 = 응급표현 사전 매칭 ↔ triage 응급도 숫자 비교.
- 예약 안전 = 데이터 유효성 검사.
- 모두 패널에 **계산 근거가 그대로 표시**("일치율 0% < 50%")되어 블랙박스가 아니고, **어떤 결과도 수의사 검토를 대체하지 않습니다.**

### Q7. (보너스) 그럼 AI는 왜 필요한가요? 그냥 수의사가 다 하면 되잖아요.
**A.** **골든타임과 업무 효율** 때문입니다. 보호자 문진을 구조화하고 차트 초안을 미리 만들어두면, 수의사는 0에서 시작하지 않고 **검토·수정**부터 시작합니다. 응급 신호가 감지된 케이스를 먼저 띄워 **우선순위**를 잡습니다. AI는 수의사를 대체하는 게 아니라, **수의사가 더 빨리·정확히 판단하도록 시간을 버는 도구**입니다.

### Q8. 그 임계값(50% 등)은 누가 정했나요? 자의적이지 않나요?
**A.** **그건 의학 기준이 아니라 운영상 검토 알림 기준입니다.**
- **판정이 아니라 알림**: 50%는 "AI 차트가 보고된 증상을 절반도 안 다루면 수의사에게 *한 번 더 보라*고 띄울지"를 정하는 값입니다. 진료를 결정하지 않으므로 임상 검증 대상이 아닙니다. (스팸 필터·이상거래 알림의 임계값과 같은 성격 — *검토 트리거*이지 *판정*이 아님)
- **보수적으로 설정**: 오탐 비용은 수의사 몇 초, 누락 비용은 진료 위험이라 **넓게(많이 알리는 쪽)** 잡았습니다(recall 우선).
- **투명**: 패널에 **"일치율 0% < 50%"** 처럼 계산 근거가 그대로 표시됩니다. 블랙박스가 아닙니다.
- **튜닝 가능**: 코드에서 `CHART_OVERLAP_THRESHOLD` 같은 **명명 상수**로 관리되어, 병원별 오탐 허용치에 맞게 조정할 수 있습니다. 고정된 진리가 아닙니다.
- 근거 문서: [VALIDATION_POLICY.md](VALIDATION_POLICY.md) §3.

> **한 문장**: "Validation은 판정기가 아니라 **검토 알림기**입니다. '스팸 임계값이 의학적으로 맞냐'고 묻지 않듯, 이건 수의사에게 한 번 더 보라고 띄우는 규칙이고 최종 판단은 사람이 합니다."

---

## 2. 30초 엘리베이터 피치 (방어용)

> "MediPaw는 진단 AI가 아닙니다. 보호자의 일상어를 구조화하고, 응급 신호를 표시하고, 차트 초안을 만들어 **수의사의 골든타임을 확보**하는 업무 보조 시스템입니다. AI 결과는 절대 그대로 쓰이지 않습니다 — **규칙 기반 검증 계층**과 **수의사 검토 절차**를 반드시 거칩니다. 판단의 주체는 언제나 사람입니다."

---

## 3. 구조 다이어그램 (발표 슬라이드용)

```
보호자(일상어)
   │  ① 구조화
   ▼
[Triage] 자연어 → 구조화 데이터 + 응급 신호 후보
   │
   ▼
[Schedule] 예약 슬롯 추천         ← AI는 여기까지 '보조'
   │
   ▼  (예약 확정)
[Chart] SOAP 차트 '초안'
   │
   ▼
[Validation] ── 가드레일(대부분 규칙 기반)
   │   A.완전성  B.문진-차트 일관성  C.예약안전  D.Red Flag '감지'
   ▼
┌─────────────────────────────┐
│   👨‍⚕️ 수의사 검토·수정·승인   │  ← 모든 의료 '판단'은 여기서
└─────────────────────────────┘
        ⟂
[Judge] 시스템 운영 품질 표본 모니터링 (audit log, 환자 안전 아님)
```

핵심 메시지: **AI는 점선 위(보조), 사람은 실선 박스(판단).**

---

## 4. 참고 프레임워크 (방어력 강화 자료)

> 수의학 진단 알고리즘이 아니라, **의료 AI의 인간 감독·가드레일·품질보증** 관점의 확립된 프레임워크에 우리 구조를 매핑한다.

### 4-1. FDA — Clinical Decision Support (CDS) Software Guidance (2022)
- **요지**: 의료진이 추천의 **근거를 독립적으로 검토**할 수 있고, 시간을 다투는 지시를 내리지 않는 소프트웨어는 규제 대상 '의료기기'에서 벗어날 수 있다(Non-device CDS). 핵심은 **투명성 + 의료진의 독립적 검토 가능성**.
- **MediPaw 매핑**: AI는 차트 *초안*과 신호 *표시*만 제공하고, 수의사가 근거(문진·키워드)를 보고 독립적으로 판단·수정한다. → "AI가 자동으로 처치를 지시한다"가 아니라 "사람이 검토하는 보조 정보"라는 포지션의 **규제적 근거**.

### 4-2. EU AI Act (Regulation (EU) 2024/1689) — Article 14 "Human Oversight"
- **요지**: 고위험 AI는 사람이 **감독·해석·무시(override)** 할 수 있도록 설계되어야 한다. 자동화 편향(automation bias)을 경계해야 한다.
- **MediPaw 매핑**: 수의사가 차트를 수정·반려·무시할 수 있고, 어떤 AI 출력도 자동 확정되지 않는다. → **Human Oversight 요건 충족 설계**.

### 4-3. WHO — Ethics & Governance of AI for Health (2021) / LMM Guidance (2024)
- **요지**: 6대 원칙 중 **인간의 자율성 보호, 안전성·투명성, 책임성**. 의료 AI는 인간의 통제 하에 있어야 한다.
- **MediPaw 매핑**: 최종 판단권을 사람에게 유지(자율성), 진단정보를 보호자에게 차단(Shadow Triage = 안전·책임), 검증 근거를 수의사에게 노출(투명성).

### 4-4. Human-in-the-Loop (HITL) / Clinical Decision Support System (CDSS)
- **요지**: CDSS와 HITL의 공통 명제 — AI는 임상가의 판단을 **증강(augment)** 하지 **대체(replace)** 하지 않는다.
- **MediPaw 매핑**: 정확히 이 패턴. AI 산출물 → 인간 검토 게이트 → 최종 결정.

### 4-5. AI Guardrails / Defense-in-Depth
- **요지**: AI 출력을 그대로 신뢰하지 않고 **다층 점검**으로 위험을 거른다.
- **MediPaw 매핑**: 규칙 기반 Validation(완전성·예약·Red Flag) + 의미 비교(일관성) + 수의사 검토의 **3중 방어**. Red Flag는 triage가 놓쳐도 규칙으로 재스캔하는 *추가* 안전망.

### 4-6. LLM-as-a-Judge (Zheng et al., NeurIPS 2023)
- **요지**: LLM을 **품질 평가자**로 쓰는 기법. 단, self-enhancement 편향 등 한계를 인지하고 *품질 평가* 용도로 한정.
- **MediPaw 매핑**: Judge는 정확히 이 용도(운영 품질 모니터링)로만 쓰고, **환자 안전 판정에는 쓰지 않는다**. 독립 인스턴스로 편향 완화.

---

## 5. 포지셔닝 제안 (어떻게 설명해야 가장 설득력 있나)

발표에서 **다음 3단 논리**로 설명하면 위 질문들이 대부분 선제 차단된다.

1. **"무엇을 안 하는가"를 먼저 말한다.**
   "저희는 진단·응급도 판정·처방을 AI가 하지 않습니다." → 심사위원의 공격 지점을 먼저 제거.

2. **"AI의 역할 = 구조화 + 신호 + 초안"으로 좁힌다.**
   "AI는 일상어를 정리하고, 위험 신호를 표시하고, 초안을 만들어 시간을 법니다." → 가치는 분명하되 책임은 안 짊.

3. **"검증 계층 + 인간 검토"를 안전장치로 제시한다.**
   "AI 결과는 규칙 기반 검증과 수의사 검토를 반드시 거칩니다(FDA CDS·EU AI Act Art.14·WHO·HITL 프레임워크와 정렬)." → 책임 구조가 견고함을 입증.

**금지 표현(자충수)**: "AI가 진단합니다", "응급도를 판단합니다", "정확도 N%로 진단", "수의사 없이도". 
**권장 표현**: "구조화한다", "신호를 감지/표시한다", "초안을 제안한다", "수의사가 최종 판단한다", "검증 계층을 거친다".

---

## 6. 시연 중 보여주면 좋은 것 (방어를 행동으로)

- 보호자 화면에 **응급도/진단정보가 안 뜨는 것**(Shadow Triage) → "보호자에겐 중립 안내만"
- 수의사 화면에 **AI 차트 초안 + 검증 표시 + '수정' 버튼** → "사람이 고친다"
- Validation의 **Red Flag '감지' 표시** → "판정이 아니라 표시"
- Judge가 **로그에만** 남는 것 → "환자 데이터 아닌 운영 지표"
