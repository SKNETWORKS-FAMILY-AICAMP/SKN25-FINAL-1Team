# Validation Agent 정책 (VALIDATION_POLICY.md)

> **한 줄 정의**: Validation Agent는 *의료 진단 검증기*가 아니라, **AI가 생성한 결과를 수의사에게 전달하기 전에 점검하는 운영상 검토 알림 계층**이다.

MediPaw는 의료 진단 AI가 아니다. 소형 동물병원의 **업무 효율화 및 골든타임 확보**를 위한 **AI 업무 보조 시스템**이다. 이 전제 위에서 Validation의 역할과 한계를 아래와 같이 명시한다.

---

## 1. 목적

AI 파이프라인(Triage → Schedule → Chart)이 만든 산출물을 **수의사에게 전달하기 전에**, 다음을 점검해 *수의사가 한 번 더 봐야 할 지점*만 표시한다:

- 필수 문진 정보가 빠지지 않았는가 (완전성)
- 문진 증상이 AI 차트 초안에 반영됐는가 (문진–차트 일치도)
- 예약이 운영상 안전한가 (스케줄)
- AI가 매긴 응급도와 응급 표현이 어긋나지 않는가 (응급신호 정합성)

Validation은 **"무엇이 옳은 진단인가"를 판단하지 않는다.** "AI 결과를 그대로 쓰기 전에 사람이 봐야 할 지점"을 표시하는 **가드레일**이다.

---

## 2. 검증 항목 (4가지) — 전부 결정론적 규칙, LLM 미사용

| 항목 | 계산식 | 경고(WARN) 조건 |
|---|---|---|
| **A. 데이터 완전성** | 채워진 필수필드 ÷ 5 (종·나이·성별·주증상·발현시점) | 5/5 미만 (누락 존재) |
| **B. 문진–차트 일치도** | 문진 증상 키워드 중 차트 본문에 등장한 비율 (문자열 매칭) | **일치율 < `CHART_OVERLAP_THRESHOLD`(50%)** |
| **C. 예약 안전성** | 예상 진료시간/예약 창 메타데이터 유효성 | 값 누락/무효 |
| **D. 응급신호 정합성** | 응급표현 사전 매칭 ↔ triage 응급도(Level) | **응급표현 감지 AND 응급도 Level ≥ `LOW_URGENCY_THRESHOLD`(4, 낮음)** |

> **★ 핵심: 4개 모두 LLM(AI)을 쓰지 않는다.** 필드 개수 / 키워드 일치율 / 숫자 등급 비교뿐이다. **같은 입력이면 항상 같은 결과(재현 가능)** 이므로 "AI가 AI를 판정한다"는 구조가 성립하지 않는다.

> **B 예시**: 문진=`구토, 설사, 식욕부진`인데 차트=`피부질환`이면 일치율 0% → WARN. 이때 Validation은 "어느 쪽이 맞다"를 말하지 않고 **"문진과 차트가 어긋남, 수의사 확인 권고"** 만 표시한다.

코드 기준: [ai/agents/validation.py](ai/agents/validation.py)

---

## 3. 임계값·기준의 성격 (가장 중요 — "그 숫자는 누가 정했나"에 대한 답)

> 이 절은 "왜 50%인가 / 누가 정했나 / 수의사 검증을 받았나" 류 질문에 대한 공식 입장이다.

1. **이 기준은 의학적 진단 기준이 아니라 운영상 검토 알림 기준이다.**
   필수 5항목은 병원 접수 양식 수준의 행정 정보, 응급표현 사전은 보호자 일상어 목록, 응급도 Level은 triage가 이미 출력한 값이다. 어느 것도 *의학적 정답*을 정의하지 않으므로 수의사의 임상 검증 대상이 아니다.

2. **Validation은 최종 판정이 아니라 수의사 검토를 유도하는 안전장치다.**
   어떤 임계값도 진료를 결정하지 않는다. "수의사에게 '한 번 더 확인' 알림을 띄울지 말지"만 정한다. 최종 판단은 100% 수의사가 한다. (스팸 필터·이상거래 알림과 같은 *검토 트리거*이지 *판정*이 아니다.)

3. **50%는 문진 내용이 차트에 충분히 반영됐는지 확인하기 위한 보수적 임계값이다.**
   "AI 차트가 보고된 증상을 절반 미만만 다루면 한 번 짚어보자"는 운영 기준이며, 의학적으로 검증된 컷오프가 아니다.

4. **오탐 비용은 낮고, 누락 비용은 크기 때문에 경고를 다소 넓게 잡았다.**
   잘못된 경고 비용 = 수의사 수 초. 놓친 불일치 비용 = 진료 위험. 따라서 *덜 거르기*보다 *넓게 알리기* 쪽으로 의도적으로 보수적으로 설정했다(recall 우선).

5. **병원별 운영 방식에 따라 `CHART_OVERLAP_THRESHOLD` 값은 조정 가능하다.**
   고정된 진리가 아니라 튜닝 파라미터다. 오탐 허용치가 다른 병원은 값을 올리거나 내릴 수 있다.

6. **패널에는 "일치율 0% < 50%"처럼 계산 근거가 그대로 표시되므로 블랙박스가 아니다.**
   수의사·심사위원 누구나 왜 경고가 떴는지 즉시 확인·검증할 수 있다. 임계값은 코드에서 **명명 상수**(`CHART_OVERLAP_THRESHOLD`, `LOW_URGENCY_THRESHOLD`)로 관리되어 매직넘버가 아니다.

---

## 4. 제한사항 (Validation이 하지 않는 것)

- ❌ "이 동물은 응급이다"를 **판정하지 않는다.** → 응급도/응급표현의 *불일치*만 알린다.
- ❌ 진단명·처방의 의학적 옳고 그름을 판단하지 않는다.
- ❌ Validation의 "이상 없음(OK)"이 "의학적으로 안전함"을 보증하지 않는다. **수의사 검토를 대체하지 않는다.**
- 규칙은 완전하지 않다(오탐·미탐 가능) — 그래서 경고 여부와 무관하게 **모든 케이스는 수의사가 검토**한다. Validation은 *추가* 안전망이지 *유일한* 안전망이 아니다.

---

## 5. AI의 역할 vs 수의사의 역할

| | AI (MediPaw) | 수의사 (사람) |
|---|---|---|
| 보호자 자연어 구조화 | ✅ | |
| 누락·문진차트 불일치·응급도 불일치 **표시** | ✅ (가드레일) | |
| 차트 **초안** 생성 | ✅ (draft) | |
| **응급도 최종 판단 / 진단 확정 / 처방 결정** | ❌ | ✅ |
| 차트 **승인·수정** | ❌ | ✅ |

→ MediPaw는 **Human-in-the-Loop** 구조다. AI 결과는 항상 수의사의 검토·수정·승인을 거친다. 최종 의료 판단의 주체는 언제나 수의사다.

---

## 6. 산출물의 흐름

Validation 결과는 `validation_result` DB에 저장되어, 수의사 EMR 화면의 **AI 사전문진 옆 "검토 권고" 영역**에 *예외(WARN)만* 표시된다(정상 항목은 숨김). 응급도·증상 자체는 응급도 배지와 문진 요약에서 이미 보이므로, 여기엔 **문진만 봐선 모를 예외**(누락·문진차트 불일치·응급도 불일치)만 띄워 수의사가 두 번 읽지 않게 한다. 보호자에게는 노출되지 않는다(Shadow Triage 정책과 일관).
