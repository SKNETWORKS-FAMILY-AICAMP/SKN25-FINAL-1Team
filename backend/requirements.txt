# FastAPI 
fastapi[standard]==0.115.0
uvicorn[standard]==0.32.0
python-multipart==0.0.12

# Database
sqlalchemy==2.0.36
alembic==1.14.0
psycopg2-binary==2.9.10  # alembic 마이그레이션(동기) 용
asyncpg==0.30.0          # FastAPI 런타임(비동기) 용
greenlet>=3.0            # SQLAlchemy async 실행에 필요
pgvector>=0.3.0  # PostgreSQL pgvector 확장 — 벡터 검색용

# JWT + 비밀번호 암호화
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# AWS S3
boto3==1.35.0

# OpenAI + LangGraph
openai>=1.54.0
langgraph==0.2.50
langchain==0.3.7
langchain-openai==0.2.9

# SSE
sse-starlette==2.1.0

# Pydantic + 환경변수
pydantic==2.9.0
pydantic-settings==2.5.0
python-dotenv==1.0.1

# HTTP 
httpx==0.27.0